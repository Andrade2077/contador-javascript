//comentarios são feitos com duas barras. Para comentar mais de uma linha usamos '*'
//Variavel = é um valor que pode ser alterado || Constante = é o valor que não pode ser alterado e não muda até o fim da solução

//var = Variavel 
//Const = Constante

//declarando variaveis e atribuindo
var preco = 2;
var desconto = 0.2;

preco = preco - desconto;

//declarando constantes e atribuindo
const PRECO = 2;
var desconto = 0.2;

var total = PRECO - desconto;
